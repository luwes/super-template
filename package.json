{
  "name": "template-extends",
  "version": "0.0.3",
  "description": "Build-less template inheritance in the browser. Handy for example or demo pages.",
  "type": "module",
  "main": "dist/template-extends.js",
  "files": [
    "src",
    "dist"
  ],
  "repository": "luwes/template-extends",
  "author": "Wesley Luyten <me@wesleyluyten.com> (https://wesleyluyten.com)",
  "license": "MIT",
  "homepage": "https://github.com/luwes/template-extends#readme",
  "bugs": {
    "url": "https://github.com/luwes/template-extends/issues"
  },
  "scripts": {
    "dev": "node test/server.js & esbuild src/template-extends.js --outdir=dist --bundle --minify-syntax --tree-shaking=true --watch",
    "build": "esbuild src/template-extends.js --outdir=dist --bundle --minify-syntax --tree-shaking=true",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "template-extensions": "^0.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.17.10"
  },
  "keywords": [
    "template",
    "template inheritance",
    "web components"
  ]
}
